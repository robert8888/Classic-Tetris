import Shape from "../Shape.js";

import shapeConfiguration from "../../../Configuration/ShapeConfiguration.js";

export default class SShape extends Shape{
    constructor() {
        super(shapeConfiguration.S);
    }

}